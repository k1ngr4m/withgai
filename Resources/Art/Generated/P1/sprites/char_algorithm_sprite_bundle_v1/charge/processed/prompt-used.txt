Use the image just shown as the visual reference for the accepted side-view algorithm engineer sprite identity, body scale, palette, proportions, and clean_hd commercial 2D game sprite style. Create a side-view hero action sprite sheet for the same algorithm engineer battle character: dark neat hair, white office shirt, blue tie, dark trousers, black shoes, slim tablet, tiny blue-purple geometric compute accents.

ACTION: charge body animation only, no projectile.

EXACT SHEET: 2 rows by 3 columns, exactly 6 equal invisible cells, no borders, no frames, no labels.

Frame sequence left-to-right across row 1 then row 2: 1 ready stance with tablet close, 2 small blue compute core begins near hand/tablet, 3 focused lean with compact geometric nodes attached near wrist, 4 stronger charge with tight blue-purple glow close to torso and hand, 5 peak charge pose with small dense compute core still inside the main silhouette, 6 hold/settle pose ready to cast.

Full body visible in every cell, side-view battle stance facing right, body-only action sheet, no detached wide VFX, no giant magic circle, no large matrix grid panel, no floating cube swarm, no projectile, no long trails. Body height and body scale should match the accepted idle/run sheets. The entire character must fit fully inside each cell, centered in the central 60% to 70% safe area, same bounding box and same pixel scale across frames, stable feet line at the same y position, leave solid magenta margin on all four sides of every cell. Background must be 100% solid flat #FF00FF magenta with no gradients. No text, no watermark, no UI, no speech bubbles.
