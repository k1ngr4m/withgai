Use the image just shown as the visual reference for the accepted side-view algorithm engineer sprite identity, body scale, palette, proportions, charge posture language, and clean_hd commercial 2D game sprite style. Create a side-view hero action sprite sheet for the same algorithm engineer battle character: dark neat hair, white office shirt, blue tie, dark trousers, black shoes, slim tablet, tiny blue-purple geometric compute accents.

ACTION: cast body animation only, no projectile and no impact.

EXACT SHEET: 2 rows by 3 columns, exactly 6 equal invisible cells, no borders, no frames, no labels.

Frame sequence left-to-right across row 1 then row 2: 1 controlled ready stance with compute core close to hand, 2 focus gesture preparing a compact logic wave, 3 release-start pose with a short attached blue-purple geometric wave near both hands, 4 peak cast pose with tight compute glow kept close to torso and hands, 5 follow-through with small attached nodes collapsing inward, 6 recovery pose returning toward idle.

Full body visible in every cell, side-view battle stance facing right, body-only action sheet, no detached wide VFX, no giant magic circle, no large matrix grid panel, no floating cube swarm, no projectile, no impact burst, no long trails. Body height and body scale should match the accepted idle/run/charge sheets. The entire character must fit fully inside each cell, centered in the central 60% to 70% safe area, same bounding box and same pixel scale across frames, stable feet line at the same y position, leave solid magenta margin on all four sides of every cell. Background must be 100% solid flat #FF00FF magenta with no gradients. No text, no watermark, no UI, no speech bubbles.
