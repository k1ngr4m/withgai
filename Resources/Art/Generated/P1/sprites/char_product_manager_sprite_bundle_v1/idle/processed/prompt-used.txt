Use the image just shown as the visual reference. Create a side-view hero action sprite sheet for the same product manager battle character, preserving identity markers: dark hair in a neat bun, glasses, beige business coat, blue shirt, dark trousers, brown office shoes, clipboard/folder held close, orange priority arrow accents, route-map card language, confident office-management silhouette. Use clean_hd commercial 2D game sprite style.

ACTION: idle loop only.

EXACT SHEET: 2 rows by 2 columns, exactly 4 equal invisible cells, no borders, no frames, no labels.

Frame sequence left-to-right then next row: 1 composed side-view ready stance facing right holding clipboard close, 2 subtle breathing and coat shift, 3 tiny attached orange priority-arrow accent pulses near the clipboard, 4 return-to-idle pose.

Full body visible in every cell, body-only action sheet, no detached wide VFX, no giant chart, no floating dashboard wall, no large route-map panel, no projectile, no long trails. The entire character must fit fully inside each cell, centered in the central 60% to 70% safe area, same bounding box and same pixel scale across frames, stable feet line at the same y position, leave solid magenta margin on all four sides of every cell. Background must be 100% solid flat #FF00FF magenta with no gradients. No text, no watermark, no UI, no speech bubbles.
